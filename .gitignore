data/.env
config/gmail_token.json
config/gmail_oauth_client.json
config/gsheets_creds.json
config/ntfy_topic.txt
config/form_responses_sheet_id.txt
config/paths.env

# Personal data — never commit; use .example templates
config/profile.md
config/target_companies.md
config/jsearch_queries.txt
config/feed_urls.txt
candidate_context/profile.md
candidate_context/master_resume.md
candidate_context/discovered_companies.md
candidate_context/discovered_companies.json
candidate_context/voice_samples/*
!candidate_context/voice_samples/README.md
candidate_context/critique_aggregate/
CLAUDE.local.md
docs/personal/
data/pipeline.db
data/pipeline.db-journal
data/pipeline.db-wal
logs/
__pycache__/
*.pyc
*.egg-info/
.DS_Store
rags/
companies/
data/connections.csv
.superpowers/
*.bak
aichat_ng_config_backup/sessions/
.DS_Store
aichat_ng_config_backup/
tmp/
docs/archive/
.claude/
.playwright-mcp/
scripts/clean_company.py
data/pipeline.db-shm

config/prefilter_rules.yaml
config/in_domain_patterns.yaml
config/companies_of_interest.txt
config/excluded_employers.yaml
config/reject_reasons.yaml
config/ranking_boost_terms.yaml
config/title_signal_keywords.yaml
config/company_aliases.yaml
.worktrees/

# Gmail integration credentials and state (#330)
config/gmail.json
config/gmail_state.json

# Per-stack pipeline config — gitignored, .example tracked
config/rapidapi_feeds.yaml
config/active_sources.txt

# Real Fly.io deploy config — gitignored, .example tracked; carries operator topology (chosen Fly app name).
# Both the canonical operator file and any per-stack variants (fly-pivot.toml, fly-staging.toml, etc).
ops/fly.toml
ops/fly-*.toml

# ── Leak prevention (#430) — categorical safety nets ───────────────────────
# Specific named files appear above; these patterns catch broader categories
# to keep operator-private / topology content out of public commits.
# When adding a new pattern here, also extend .git/hooks/pre-commit if the
# leak shape is content-based rather than filename-based.

# Per-checkout / per-operator local files (.local.* convention; matches CLAUDE.local.md)
*.local.md
*.local.json
*.local.yaml
*.local.yml
*.local.txt
*.local.sh
*.local.toml
*.local.env
*.local

# Secrets / credentials (any depth)
.env
.envrc
.envrc.*
.secrets
*.secrets
*.secret
.netrc
.netrc.*
_netrc
*.netrc
secrets.yaml
secrets.yml
secrets.json
secrets.txt
*.backup

# SSH / PGP / TLS material
.ssh/
id_rsa*
id_ed25519*
id_ecdsa*
id_dsa*
*.pem
*.p12
*.pfx
*.gpg
*.asc

# Operator-private scratch / personal directories
private/
_private/
personal/
_personal/
scratch/
_scratch/
_local/
.local/
notes/

# PII-sweep findings storage (#430-style work — findings stay operator-private)
_audit/
_findings/
pii-findings*
*-findings.md
*-findings.txt
*-leak-scan.*

# Implementation plans, specs, and experiments (#430) — operator-private.
# These are session-execution diaries written for ralph-loop / Claude Code,
# not pedagogical artifacts. Keeping them out of public commits eliminates
# the largest topology-leak surface in the repo. Files stay on disk for
# local session use; just no longer tracked. See CLAUDE.md "Plans and specs"
# rule for placement guidance.
docs/superpowers/

# Editor / IDE state (may contain absolute paths or partial edits)
.idea/
.vscode/
*.swp
*.swo
*~

# OS metadata (reveals local FS structure)
Thumbs.db
desktop.ini

# Jared session locks
.jared/

# Test / build cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
dist/
build/
