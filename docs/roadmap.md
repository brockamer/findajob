# Project roadmap

Canonical phase-narrative for findajob. Captures *why* the work is ordered the way it is — the board captures *what* is being worked on right now.

See [`project-board.md`](project-board.md) for the full division of labor between this doc and the board. Short version: phase ordering, cross-issue decisions, and milestone-level acceptance live here. Issue state, per-issue acceptance, and dependencies live on the board.

If a roadmap fact drifts between this doc and an issue, *this doc wins*.

---

## Active phase — Post-Launch User Sunset anchor (Decision 29, 2026-05-23)

GA shipped 2026-05-04 (Decision 20); Multi-Tenancy Foundations shipped 2026-05-06 (Decision 21); Cost + Credentials Hardening and OpenRouter Native Migration shipped between 2026-05-06 and 2026-05-09 (Decision 22); Reduce per-prep cost and Board Transition Coherence shipped during the v0.27.x arc (Decision 27); **Fly.io Public Launch and Release Validation both shipped 2026-05-23 (Decision 29)** — FPL closed once #810 and #377 (Epic) closed; RV closed after 11/12 items shipped in 5 days and #777 was reassigned to Audit & Cleanup. The 2026-05-23 reshape (Decision 29) flipped the active anchor to **Post-Launch User Sunset** (m26, due 2026-06-15) — the chronologically-next milestone, with its migration-tool blocker (#816) closed 2026-05-23 and three pullable sub-issues remaining (#749, #817, #818, #819).

**Active goal:** ship Post-Launch User Sunset to its deliverable sentence (see [Post-GA horizon](#post-ga-horizon) below). Self-onboarded docker stacks get migrated to per-user Fly accounts via the now-shipped migration tool; local docker reduces to dev/pre-tag tiers only.

Six milestones deferred 30 days per Decision 25 (Funnel + Triage UX, Ops Hardening, Tools Page Maturity, Stats Dashboards, Audit & Cleanup, Tuning Loop) — none abandoned; their issues stay milestoned. The unfreeze window (~2026-06-15) lines up with Post-Launch User Sunset's due date, at which point the close-out arc concludes and the deferred backlog thaws. **Funnel + Triage UX (m9, 2026-06-24)** is the chronologically-next milestone after PLTS and the natural successor active anchor.

---

## Shipped milestone: General Availability

[Milestone 4 · closed 2026-05-04, ahead of due 2026-05-12](https://github.com/brockamer/findajob/milestone/4)

**Goal.** A second person (the first external user) runs the pipeline independently in a different field. Config layer fully externalized, user docs written, onboarding flow exists.

**Milestone-level acceptance** (all must hold to close):

1. External user is deriving daily utility — not merely testing.
2. Her data survives code updates with zero admin action on her part.
3. Web frontend has retired Sheets reads (Dashboard/Applied/Review/Waitlist), writes (STATUS/REJECT_REASON), manual JD ingest, and Google Drive materials viewing.
4. Apply-gate stayed met on average during the arc (daily applies ≥ 1/day averaged across any 7-day window).

### Remaining scope

Phase labels were retired in Decision 17 (2026-04-29) — work is now organized by milestone with deliverable sentences, not by phase. The phase-arc that got GA to its current near-shippable state is preserved in shipped-and-retired form below for historical context.

Currently open in GA: a stripped-down sprint focused on user-onboarding correctness. Most polish/follow-up was reassigned to other milestones in the 2026-05-01 structural review (see Decision 19).

```
GA sprint scope (post-2026-05-01 reshape):
  - #345  Discoverer regex matches onboarding profile.md schema (real bug)
  - #84   excluded_employers config + prefilter enforcement
  - #76   Docs refresh for Docker deploy
  - #275  gmail_linkedin docs expansion
  - #373  Tracking: external user onboarding (acceptance proof)
```

### Shipped phases (historical)

```
Phase 1 (shipped)          Config externalization + frontend evaluation
Phase 2 (shipped)          Docker migration + release management
Phase 2.5 (shipped)        v0.1.0 tag cut (48h dogfood gate)
Phase 3 (shipped)          Web materials viewer (retired rclone + Drive)
Phase 4 (shipped)          First external deployment (2026-04-29 — 2026-04-30)
Phase 5 (shipped)          Web-frontend sub-phases: read-only views,
                           STATUS/REJECT workflows, manual JD ingest, stats/trends
Phase 6 (in-flight)        User-facing documentation — partially shipped via /docs/;
                           remaining sweep tracked under #76 + #275
```

Phase ordering rationale preserved in Decisions 8 (Phase 3 before Phase 4) and 11 (onboarding interview critical path to Phase 4).

### Decisions

Append-only. Numbers are stable references. Amend an entry in place only for factual corrections (renamed file, etc.); supersede with a new entry otherwise.

1. **rclone replacement — tabled.** Accepted pending the Phase 3 materials viewer, which retires the rclone layer entirely. No tactical hardening pass in the interim.
2. **User deployment workflow — image-pull cycle.** Bind-mounted `state/` subtree for data, image pulls for code. Deploy updates with `docker compose pull && docker compose up -d`. User data never at risk from updates.
3. **Auth — the perimeter VPN perimeter only.** No session auth in the app, no TLS code, no public exposure. See [`deployment-model.md`](deployment-model.md) for topology.
4. **Timeline — 3–5 weeks full arc.** Contingent on: scope holds, apply-gate stays met daily, no surprise complexity in the board-write pivot (14c / #61 — `poll_flags.py` replaced by web handlers + `watchdog.py` 2026-04-22).
5. **Phase 1 parallelizes** via `superpowers:dispatching-parallel-agents` + ralph-loop (mechanical config-externalization work) + `frontend-design` (Phase 5 viewer).
6. **Registry + image-tagging:** GHCR. `:main-<sha>` on merge, `:latest` floating, `:vX.Y.Z` immutable on git tag, plus moving alias `:vX.Y` repointed on each patch. Every stack runs `:latest`; immutable tags exist as an audit trail and per-stack freeze affordance.
7. **Release ownership:** Claude orchestrates releases (tagging, CHANGELOG, notes, migration markers, dogfood verification); user reviews and approves. See [`release-process.md`](release-process.md).
8. **Phase reorder — Phase 3 before Phase 4.** Shipping an external user without materials access was a fake win. The web materials viewer was promoted to Phase 3 so the first external user's first pull has working materials access with zero rclone on her side. Operator keeps rclone through the Docker migration (env-var gated) — deleted when Phase 3 lands.
9. **aichat-ng build strategy:** pull prebuilt musl binary from `blob42/aichat-ng` GitHub Releases pinned to a tag; no Rust toolchain in image. Fallback to source build is a one-line change if the fork goes stale.
10. **Scheduler inside container:** `supercronic` running a crontab that mirrors the old systemd timers 1:1. Timezone set per instance via `TZ=America/Los_Angeles`. On-demand scripts via `docker compose exec`.
11. **Onboarding interview — Phase 4 prerequisite.** Hand-curating a second user's candidate config is untenable; the interview is critical path to Phase 4.
12. **v0.1.0 dogfood gate discipline:** during the 48h observation on `:latest`, any merge to `main` restarts the clock. Phase 3 + Phase 4 work happens on unmerged branches.
13. **User-facing documentation — split planned.** The umbrella scope is too broad for one issue. Plan: three focused issues, each paired with the shipping work that makes the doc concrete — (a) day-1 Dashboard usage guide paired with Phase 4 external deploy, (b) tuning guide paired with first feedback-calibration cycle, (c) troubleshooting guide paired with first real user failure.
14. **Post-GA milestone split (2026-04-24).** Post-GA Hardening grab-bag replaced with four dated release milestones (v1.1 / v1.2 / v1.3 / v1.4 — see "Post-GA horizon" below). Driven by a structural review: the single grab-bag milestone couldn't pass the one-sentence-deliverable test, and the Roadmap view showed no visible arc past GA. v1.4 (Funnel + Triage UX) precedes v1.2/v1.3 because active-operator triage friction is the binding constraint on the daily job-search loop.
15. **Umbrella-epic convention (2026-04-24).** GitHub's native sub-issue field is the canonical parent-child relationship for grouping related work (example: #228 tuning loop → #229/#230/#231; #239 credentials hygiene → #67/#225; #240 cost observability → #48/#87). Epics are `enhancement`-labeled, Medium priority, and live on the board like any other issue — not labels, not milestones. Milestones are release boundaries; epics are thematic groupings that may span milestones.
16. **Optional HTTP Basic Auth for internet-exposed instances (2026-04-28, supersedes #3 for the public-exposure case).** A FastAPI middleware (`findajob.web.auth`) gates the entire web UI behind shared-secret HTTP Basic Auth when `FINDAJOB_AUTH_USER` / `FINDAJOB_AUTH_PASS` env vars are both set, with `/healthz`, `/static/*`, and `/favicon.ico` allowlisted. When env vars are unset the middleware is a no-op — the perimeter VPN-only deployments are unchanged. Intended for per-user instances at `findajob-{handle}.example.com`; defends against drive-by scanning, not against a determined adversary. Real per-user identity / RBAC remains out of scope (still a future change). See `docs/operations/internet-exposure.md` (#327).
17. **Multi-tenancy promoted from wishlist to v0.9 milestone, post-GA dates compressed, phase labels retired (2026-04-29).** Structural review on 2026-04-29 made four changes. (a) Multi-tenancy was reframed from "far-future wishlist" to urgent — operator has multiple users ready to onboard but the platform isn't quite there yet. New milestone v0.9 — Multi-Tenancy Foundations (between GA and v1.1) anchored by [Epic] #338, parenting #330 / #333 / #336 / #339. (b) All post-GA milestone dates compressed 2-3x to reflect actual ~80-issue/week shipping pace observed in the prior 14 days. (Dates further compressed in Decision 18 the same day — see below.) (c) Phase labels (`phase-4` / `phase-5` / `phase-6`) retired entirely — vestigial after Decision 14's milestone-based reorganization; stripped from all open issues and deleted from the repo. (d) PII drift cleanup: GA milestone description previously named the operator's first external user; redacted to generic "the first external user" per the lowercase-handle convention adopted 2026-04-28 in CLAUDE.local.md. Six closed-but-stuck items (#15, #16, #86, #88, #252, #303) bulk-moved to Done; #228 renamed with `[Epic]` prefix per Decision 15.
18. **Second date compression + version-codename convention recorded (2026-04-29 — same day as #17).** Structural review later 2026-04-29 made four changes. (a) Dates compressed again, this time pulled into a single ~30-day window targeting last milestone (v1.3) at 2026-05-29. New dates: GA 2026-05-12; v0.9 2026-05-18; v1.1 2026-05-22; v1.4 2026-05-25; v1.2 2026-05-27; v1.3 2026-05-29. Justification: the previous compression in #17 was still conservative relative to actual shipping cadence; #17's dates were anchored on calendar-month feel rather than throughput. (b) Version-numbering convention recorded: chronological release order is GA → v0.9 → v1.1 → v1.4 → v1.2 → v1.3 — v1.4 ships before v1.2 / v1.3 per Decision 14's "funnel-first" framing. **Versions are codenames, not semver** — readers should not assume v1.2 ships after v1.1. (c) Bundle of mechanical drift fixes and milestone moves applied: #186 (TLS+auth proxy) removed from GA — Decision 16 already shipped Basic Auth as the practical answer, remaining scope is far-future big-idea no-milestone. #344 (multi-tenant scheduler stagger) moved v1.3→v0.9 (it's multi-tenant work). #211 / #212 / #215 (onboarding paste-back polish) moved v1.4→v0.9 (strengthens v0.9's onboarding deliverable). #275 (gmail_linkedin docs) moved v1.4→GA (paired with user onboarding). #345 (discoverer regex bug) given GA milestone (real bug affecting user onboarding output). #150 (`/tools/` page) lost vestigial `big-idea` label (concrete Phase 1 implementation already in CLAUDE.md). (d) v1.4 deliverable rewritten to span both halves: "More job sources flow into the funnel, and the operator's daily triage loop makes every candidate row actionable in one click with prior-application context inline." Title was already "Funnel + Triage UX" but description was triage-only.

20. **Structural review (2026-05-04): GA shipped + minor reshape.** Five changes. (a) **GA milestone closed** — all 5 stripped-down sprint issues (#345, #84, #76, #275, #373) shipped; the first two external users onboarded and deriving daily utility per the milestone-level acceptance from §1. Active milestone shifted to Multi-Tenancy Foundations (the next-due milestone after GA). (b) Three real-work milestone-orphans assigned: #429 (reject-reason taxonomy generalization + drift fix) → Funnel + Triage UX; #428 (CLAUDE.md data-ownership table) → Ops Hardening; #425 (cron stagger collisions) → Multi-Tenancy Foundations (sibling of #344). (c) Drift fix: #342 (programmatic NotebookLM investigation) downgraded from Priority=High → Priority=Low to reconcile with its `big-idea` label (per project-board.md "big-idea always pairs with Priority=Low"). (d) Date-clustering risk surfaced for awareness: 3 milestones now stack within 4 days (Funnel+Triage 5-25, Tuning Loop 5-27, Ops Hardening 5-29) — realistic at observed shipping cadence but fragile to a slip cascade. No date adjustment proposed. (e) Future arc confirmed orphan: #378 (outcome-driven self-tuning) and #379 (cross-user signal aggregation) stay `big-idea` + milestone-orphan until OS Launch readiness (#377) ships and the post-launch arc can crystallize. Dependency graph remains clean (5 edges, 4 issues, 0 cycles, 0 priority inversions).

19. **Structural review (2026-05-01): rename milestones, scope-tighten GA, file future arc.** Five changes. (a) Milestones renamed to drop `v0.9 / v1.1 / v1.2 / v1.3 / v1.4` codename prefixes after v0.9.x release tags began shipping work from the v0.9 milestone — the codename-vs-semver collision called out in Decision 18 had become an actual naming bug. New names: `Multi-Tenancy Foundations`, `Cost + Credentials Hardening`, `Funnel + Triage UX`, `Tuning Loop + Stats`, `Ops Hardening`. Semver release tags continue floating on the release-process schedule independently. (b) GA scope-tightened from 10 issues to 5 (#345, #84, #76, #275, #373). Six polish/follow-up issues moved to other milestones: #181/#126/#301 → Ops Hardening; #283/#287 → Multi-Tenancy Foundations; #85 → Funnel + Triage UX. GA acceptance criteria already largely met by the first two external users (deployed 2026-04-29 — 2026-04-30). (c) Five real-work milestone-orphans assigned: #358 → Funnel + Triage UX; #359/#360 → Ops Hardening; #362 → Funnel + Triage UX (has fresh design spec from today); #372 → Multi-Tenancy Foundations. #373 promoted from `tracking`-labeled to `enhancement,open-source` and assigned to GA — getting external users onboarded IS the GA acceptance test. (d) Phase 5/6 framing in GA Active milestone section retired per Decision 17; replaced with "Remaining scope" + "Shipped phases (historical)" subsections. (e) Future arc filed as three big-idea issues on the board: #377 (Open-source launch readiness epic), #378 (Outcome-driven self-tuning), #379 (Cross-user signal opt-in telemetry). Far-future wishlist section refreshed to point at issue numbers and to acknowledge that "Multi-user scaling" was promoted out of wishlist on 2026-04-29. (f) **Reverted later same day:** an earlier draft of this decision retired the `Up Next` and `In Progress` Status columns. Reverted because the jared plugin is designed around the full `Backlog → Up Next → In Progress → Blocked → Done` flow; flattening to `Backlog → Done` broke alignment with the plugin's flow primitives. Sticking with the canonical 5-column model.

21. **Structural review (2026-05-06): MTF shipped + OpenRouter Native Migration milestone added.** Four changes. (a) **Multi-Tenancy Foundations closed** ahead of due 2026-05-18 — all external users onboarded with isolated credentials; in-app onboarding chat, multi-tenant operator dashboard, per-stack Gmail IMAP, cron stagger all landed. Active framing shifted to **parallel close-out** of five concurrent open milestones (CCH, OpenRouter Native Migration, Funnel + Triage UX, Tuning Loop + Stats, Ops Hardening); singular `## Active milestone:` section retired in favor of `## Active phase — parallel close-out`. (b) New milestone **[OpenRouter Native Migration](https://github.com/brockamer/findajob/milestone/12)** (m12, due 2026-05-10) anchored by epic #469 with Phase 1/2/3 children #470/#471/#472. Deliverable: aichat-ng fully replaced by `findajob.llm.openrouter`; prompt caching active on Opus-4.7; `cost_log.cost_usd` from `response.usage.cost`; calibration multiplier stack retired. Subsumes #463 in Phase 2; eliminates the heuristic-+-multiplier hack from Decision-log neighbors #87/#467. Tight 4-day window deliberately chosen by operator. (c) Drift fix: 4 unmilestoned non-big-idea issues triaged — #446 (emergent reject-category prompt) → Funnel + Triage UX; #462 (dead cost-extraction code) → CCH; #463 left open as tracker (will close when #471 lands); #434 ("[BUG] No Dark Mode" satire) closed as joke. (d) Date-cluster risk re-noted from Decision 20: 3 milestones still stack 5-25/5-27/5-29; OpenRouter Native Migration adds a 4-day milestone at 5-10. Operator chose to leave dates alone — observed shipping cadence supports it, but slip-cascade fragility persists. Dependency graph clean (4 edges, 3 issues, 0 cycles, 0 priority inversions). 11 unmilestoned items remaining are all `big-idea` or `personal` (correctly orphaned per project-board.md convention).

22. **Structural review (2026-05-09): post-arc reshape — CCH + OpenRouter Native Migration shipped; M-series code-health track surfaced; OS Launch Readiness reconciled.** Six changes. (a) **Cost + Credentials Hardening (m6) and OpenRouter Native Migration (m12) closed** — both shipped between 2026-05-06 and 2026-05-09, ahead of or on their dues (CCH 10/0 due 2026-05-22; OpenRouter Native Migration 4/0 due 2026-05-09). Active parallel close-out reduced to four concurrent open milestones (Funnel + Triage UX, Stats Dashboards, Ops Hardening, Tuning Loop), with Open-Source Launch Readiness milestoned in advance as the named post-arc horizon. (b) **M-series code-health track surfaced retroactively.** Six small refactor/quality milestones had shipped invisibly to this narrative since the previous review — Test Baseline (m15, 8/0), DB Helper + utils.py Split (m17, 4/0), Versioned Migration Runner (m18, 1/0), Background-Task Observability (m19, 1/0), Type-Gate Tightening (m20, 1/0), Pipeline-Script Extraction (m16, 5/0 — closed today as part of this review). Listed in `## Shipped and retired milestones` with a one-line entry per ship; convention going forward is one-line acknowledgement rather than full milestone section, since the scope is mechanical and the narrative weight is low. (c) **OS Launch Readiness drift resolved (option a).** The milestone (m11, due 2026-06-29) had been promoted to active in advance of Ops Hardening shipping — the trigger condition in Decisions 19/21's wishlist framing — so the Far-future wishlist text and the milestone description had drifted (the description even claims "Promotes #377 from big-idea to active roadmap"). Wishlist entry rewritten to acknowledge promotion-in-advance; milestone keeps its date and 4 issues. (d) **Real-work orphan assignment.** Seven open issues had no milestone — #565 / #567 / #572 → Ops Hardening; #571 / #585 / #601 → Funnel + Triage UX; #606 → Stats Dashboards. The dev-pipeline-uplift pair (#565 / #567) was folded into Ops Hardening rather than standing up a new milestone, keeping milestone count low at the expense of a slightly broader Ops Hardening sentence. (e) **Drift fixes.** #496 Priority Medium → Low (big-idea convention); #9 closed as obsolete (m18 Versioned Migration Runner shipped its substance); #601 received `enhancement` label and #606 received `bug` label (both had been label-empty). (f) **Ops Hardening deliverable sharpened** — appended "and write paths have e2e coverage" to the milestone description, catching #181 (Playwright board e2e) explicitly within the deliverable sentence's scope. Dependency graph remains clean (3 issues, 4 edges, 0 cycles, 0 priority inversions, 0 orphans; critical path #231 → #230 → #229, the Tuning Loop C.0/C.1/C.2 chain). 11 unmilestoned items remaining are all `big-idea` (correctly orphaned per project-board.md convention).

23. **Structural review (2026-05-11): board-add discipline catch + Reduce per-prep cost promoted.** Five changes. (a) **13 open issues found missing from the board** — filed via `gh issue create` but never `gh project item-add`'d. All 13 added with per-issue Priority and Status=Backlog: #625 (DB corruption) → Ops Hardening High; #623 (onboarding credit-exhaustion) → OS Launch Readiness High; #620 (feedback widget 500) → Funnel + Triage UX Medium; #630 (clean-stack reset) → Ops Hardening Medium; #614 (/settings/connections/ follow-up) → Funnel + Triage UX Medium; #619 (HTMX-aware defense) → Ops Hardening Low; #606 and #601 board-add only (already milestoned per Decision 22); #622 board-add only (already in Reduce per-prep cost milestone); #640 (per-prep cost spike) → Reduce per-prep cost Medium + `enhancement` and `cost` labels; #496 (parallel prep) → Reduce per-prep cost Low; #636 + #639 not added — both closed as already-fixed (88518d6 and c407945 respectively; acceptance criteria matched directly). (b) **Reduce per-prep cost (m21) promoted from off-roadmap orphan to active milestone.** Previously had 1 issue (#622), no due date, no entry in §Post-GA horizon. Now has 3 issues (#622 / #640 / #496), due 2026-06-05, deliverable sentence written, listed alongside the other four parallel-close-out milestones. Total active milestones for the close-out arc rises from four to five. (c) **Two fixed-but-never-closed bugs reconciled** — #636 (fix in 88518d6) and #639 (fix in c407945 self-marked complete in issue body) closed with explanatory comments. The pattern (fix lands → issue body never updated → "OPEN" lingers) is a side-effect of filing bug-then-fix in tight succession without a board-add step closing the loop. (d) **Doc-link drift fix in `docs/maintainers/project-board.md`** — four broken relative links `[roadmap.md](roadmap.md)` corrected to `../roadmap.md` (the doc was relocated to `docs/maintainers/` at some prior point but its outbound links weren't fixed up); broken `[deployment-model.md](deployment-model.md)` reference repointed at the existing `../architecture.md`, since that file is the actual canonical home for the deployment topology table the row promised. (e) **Up Next staleness noted but left as-is per operator preference** — #124 and #188 both carry `[needs rethinking]` title prefixes but remained in Up Next at operator's call (Bundle D declined). Dependency graph remains clean (3 issues, 4 edges, 0 cycles, 0 priority inversions, 0 orphans; critical path #231 → #230 → #229 unchanged). Shape stays one coherent board (49→47 open after the two closes; no orthogonal work streams; no split signal). 7 unmilestoned items remaining are all `big-idea` (correctly orphaned per project-board.md convention).

24. **Structural review (2026-05-14): orphan absorption + Stats Dashboards date relief + two new milestones (Tools Page Maturity, Audit & Cleanup).** Six changes. (a) **10 real-work orphans assigned** — all filed 2026-05-13/14 within 48hrs of Decision 23's sweep; same Decision-17/19/22/23 recurring failure mode (issues filed via `gh issue create` without milestone). Routing: #659/#661 → Funnel + Triage UX (Workday-tenant IMAP filter extension + #362 review-queue reattribute bug); #648 → Open-Source Launch Readiness (Fly.io hosted-deploy validation); #650/#653/#654 → new Tools Page Maturity milestone; #641/#649/#651/#662 → new Audit & Cleanup milestone. (b) **Two new milestones filed.** Tools Page Maturity (m22, due 2026-06-02): deliverable — "The /tools/ page evolves from a prompt-tile launcher into a working operator surface: an interactive exclusion rule editor (#653), manual operator controls + pipeline.jsonl log viewer (#650), and verified Open-in-Claude prefill behavior (#654)." Audit & Cleanup (m23, due 2026-06-12): deliverable — "Half-working notification surfaces, dead-code drift, release-process cruft, board state-transition incoherence, and accumulated capability-without-UI gaps are surfaced and either fixed in-place or filed as concrete follow-ups." Convention follows the M-series acknowledgement pattern from Decision 22 — small focused milestones surface accumulated audit-class and surface-maturity work without inflating the active arc's headline scope. Total active milestones for the close-out arc rises from five to seven (eight including OS Launch Readiness milestoned-in-advance). (c) **Stats Dashboards date slipped 2026-05-27 → 2026-06-10.** Milestone was 0/5 closed with 13 days to ship and concurrent Funnel + Triage UX absorbing 3 new items; calendar reality dictated the slip. #606 (cost burn-rate widget bug) moved out of Stats Dashboards into Funnel + Triage UX (it's an operator-facing bug not a dashboard; remaining four /stats/* pages stay grouped). (d) **#572 Blocked → Backlog drift fix.** Its hard dependency #565 (populated soak instance) closed 2026-05-10, so the Blocked state was stale — the dynamic-walkthrough mechanism is now unblocked, the issue is simply unscheduled. Moved to new Audit & Cleanup milestone. (e) **#663 [Tracking] Hosted/PaaS deployment patterns filed as `big-idea` orphan.** Anchored today by #648's Fly.io validation work; promotes out of `big-idea` only if external users ask for hosted variants enthusiastically (parallel to the wishlist `Hosted variant` framing). (f) **Up Next stale-count noise persists** — sweep reports 5 items but `gh project item-list` filter shows 1 actively in Up Next; appears to be a sweep cache or counting-mode artifact rather than real drift. No action. Dependency graph remains clean (3 issues, 4 edges, 0 cycles, 0 priority inversions, 0 orphans; critical path #231 → #230 → #229 unchanged). Shape stays one coherent board (53 open issues including the new tracker #663; no orthogonal work streams; no split signal). 9 unmilestoned items remaining are all `big-idea` (correctly orphaned per project-board.md convention).

25. **Structural review (2026-05-15): Fly.io Public Launch promoted to single active anchor; six milestones deferred 30 days.** User instruction to orient all labor around getting findajob to "average person on Fly.io" — first deployed and validated by operator (#648), then ready for an unaffiliated stranger to use without operator support. Seven changes. (a) **Open-Source Launch Readiness (m11) renamed and refocused as Fly.io Public Launch.** Deliverable rewritten from `docs/setup/install-docker.md` self-host framing → Fly-first average-user path; date pulled 2026-06-29 → 2026-05-29; epic #377's body updated to match. The original OSLR deliverable was already drifted — Decision 24 had wedged #648 (Fly validation) into it without widening the sentence. The rename fixes that drift. Self-host docker-compose remains documented as a power-user surface but is no longer the strategic anchor. (b) **Six concurrent milestones deferred 30 days:** Funnel + Triage UX 2026-05-25 → 2026-06-24; Ops Hardening 2026-05-29 → 2026-06-28; Tools Page Maturity 2026-06-02 → 2026-07-02; Stats Dashboards 2026-06-10 → 2026-07-10; Audit & Cleanup 2026-06-12 → 2026-07-12; Tuning Loop 2026-06-15 → 2026-07-15. None abandoned — their open issues stay milestoned. New work simply does not pull from them until Fly.io Public Launch ships. Reduce per-prep cost (m21, due 2026-06-05) stays active in parallel because runaway cost is a direct blocker on "average person can afford this." (c) **Active framing shifted** from parallel close-out of seven concurrent milestones to single active anchor (Fly.io Public Launch) + cost-defense sibling (Reduce per-prep cost) + six deferred. Chronological release order updated to reflect the leapfrog. (d) **Six new sub-issues filed (#669–#674)** under epic #377, all milestoned to Fly.io Public Launch: #669 install-fly.md polish (Medium, blockedBy #648); #670 cost transparency doc (High, blockedBy #671); #671 cost-cap onboarding feature (High); #672 unaffiliated-user walkthrough (High, blockedBy #669/#670/#671/#674/#648 — the milestone acceptance test); #673 README pivot (Medium, blockedBy #669); #674 API-key acquisition audit (Medium). Three High items reflect adoption-blocker scope; "High is scarce by design" relaxed deliberately for this milestone. (e) **#648 promoted from Backlog/Medium to In Progress/High** — its body declares zero dependencies, so it is the green-light validation that gates the rest of the new sub-issues. (f) **#663 ([Tracking] Hosted/PaaS deployment patterns) closed as redundant** — its scope was absorbed into the refocused milestone plus the new sub-issues; additional PaaS targets (Render / Railway / managed-K8s / hosted variant) remain off-roadmap until external users explicitly ask. (g) **Hosted/managed-service variant explicitly out of scope** per operator instruction (no time for managed-service work right now); remains in §Far-future wishlist as no-issue-filed. Dependency graph adds 8 new edges bound to the new sub-issues + #648; critical path of the new graph is #648 → #669 → #672 (3 nodes; parallel chain #671 → #670 → #672 also 3 nodes). 0 cycles, 0 priority inversions among the new edges. Shape stays one coherent board (56 open issues after the close + 6 files; no orthogonal streams; no split signal). The structural review proposal-and-bundle flow executed inline: 7 bundles proposed, all 7 approved, all 7 executed in one session.

26. **Structural review (2026-05-16): post-launch user sunset arc captured; release process scope widens to dual-track.** Operator instruction during this reshape made the post-Fly.io-launch deployment topology explicit. Six changes. (a) **User sunset path codified.** Once Fly.io Public Launch ships (m11, due 2026-05-29) and self-service deploy is verified by the #672 acceptance test, the self-onboarded user stacks on `<deployment-host>` are sunset. Users will be told to sign up for their own Fly.io accounts; a data-migration mechanism (TBD — new issue) will export their stack's `data/` + `companies/` + `candidate_context/` and import to their new Fly app; their docker stacks are then stopped. No user moves to Fly self-service until the migration tool exists and has been operator-verified. (b) **The first external user stays under operator administration**, but on operator's Fly account rather than on `<deployment-host>`. Operator + first external user are the only two stacks running under operator's Fly account post-sunset. (c) **Release process widens to dual-track** until user sunset completes: every release ships both the docker image (for users still on docker pin discipline) *and* the Fly app update (for operator + the first external user). Post-sunset, the docker-update step drops out of the release runbook for the user cohort but remains for the two local development tiers (factory-fresh test instance + populated soak instance), which stay operator-side as dev/pre-tag infrastructure only — operator's job-search runs from Fly from that point on, local docker is dev-only. (d) **Board-staging action:** Up Next reset to the #672 blocker chain (#669 install-fly.md polish, #670 cost transparency doc, #674 API-key audit) — the three concrete blockers gating the milestone-acceptance test, with #672 staying in Backlog until its blockers clear. Three deferred-milestone items previously in Up Next (#653 Tools rule-editor, #641 ntfy audit, #649 release-process audit) demoted back to Backlog per Decision 25's deferral. (e) **Routine grooming bundles in same session.** Filed #687 (chore: normalize acceptance-criteria format across 26 backlog issues — they use non-canonical `## Acceptance criteria` instead of the `<details><summary>Expand</summary>` wrapper, which silently fails the /jared-stage filter). 9 unmilestoned issues assigned to existing milestones (3 to Fly.io Public Launch — #676 / #677 / #684; 1 to Reduce per-prep cost — #478; 2 each to Funnel + Triage UX — #249 / #678 — and Tuning Loop — #265 / #268; 1 to Audit & Cleanup — #668). 5 speculative issues labeled `big-idea` (#94, #342, #378, #411, #665). Earlier in the session: 4 stale `blockedBy` edges removed (#669↛#648, #670↛#671, #672↛#648, #672↛#671 — all four blockers closed during the 2026-05-15/16 release arc); #565 dev-pipeline plan archived. (f) **Open follow-ups (not filed this session; surface when #672 is ~1 week from acceptance):** (i) data-export/import migration tool for user stack → Fly app, (ii) release runbook update to dual-track docker + Fly, (iii) user comms explaining the cutover and Fly signup expectation, (iv) post-sunset cleanup issue to stop and archive the docker stacks on `<deployment-host>`. Dependency graph re-checked: 5 issues, 8 edges, 0 cycles, 0 priority inversions, 0 orphans; critical path #231 → #230 → #229 still the longest (Tuning Loop, deferred). Shape stays one coherent board (57 open issues after #687); the post-sunset state remains one project — fewer deployment tiers, same scope. Strategic-goal endpoint question (what happens to findajob when operator gets hired) left explicitly unaddressed per operator instruction.

27. **Structural review (2026-05-19): Release Validation milestone added; priority overhaul aligned with major-release readiness; Docker + Fly parity tracker filed.** Operator instruction: "manage relative priorities so that some issues are hi PRI that deserve it considering the most important things right now are to get the fly.io deployment to the point that any person off the street can run it if they sign up for a fly account, and to thoroughly validate all the UI paths and features and overall ensure findajob is ready for a major point release and deployment to both docker and fly.io as flawlessly as possible." Seven changes. (a) **Two shipped milestones closed:** Board Transition Coherence (m24, 0/9, due 2026-06-12) and Reduce per-prep cost (m21, 0/4, due 2026-06-05) — both had zero open issues; the deliverables shipped during the v0.27.x arc but the milestones were never marked closed. (b) **New milestone Release Validation (m25, due 2026-06-05)** filed alongside Fly.io Public Launch. Deliverable: "Every UI path on the operator's docker-compose stack and a fresh Fly.io deploy passes the loose-ends audit; the unaffiliated-user walkthrough (#672) completes without ship-blocker findings; docker-compose deploy from a clean directory reproduces the same surface behavior as a fresh Fly deploy." The milestone covers what Fly.io Public Launch does not — UI/feature validation across both deploy substrates as the gate for a major point release. The operator initially requested the name "v0.30 Release Validation"; renamed to "Release Validation" mid-session to conform to Decision 19 (2026-05-01) which retired version-codename milestone names after `v0.9`-style names began colliding with `v0.9.x` semver release tags. The descriptive-only convention from Decisions 19/21/22/24 (Audit & Cleanup, Tools Page Maturity, Ops Hardening shape) stands. (c) **Six orphan-no-milestone issues assigned to Release Validation:** #738 (continue-prep UX confusion), #743 (onboarding stream abort), #736 (#572 phase-2 audit run), #735 (phase-2 --output-dir), #734 (phase-2 REVIEW findings dropped), #729 (UI surface for excluded_employers — moved out of Audit & Cleanup since it's a specific gap blocking the release-readiness story). #737 (openrouter wrapper refactor) routed to Audit & Cleanup as post-launch tech debt. The four remaining orphans (#94, #342, #378, #665) all carry the `big-idea` label and stay correctly orphaned per project-board.md convention. (d) **Priority overhaul.** Tier 1 promote to High: #746 (jobs-api14-indeed allowlist — blocks non-engineering candidates per #672 dry-run finding), #673 (README Fly-first pivot), #738 (continue-prep UX), #729 (excluded_employers UI), #743 (onboarding stream abort), #736 (#572 phase-2 audit run). Tier 2 promote to Medium (from NoPri): #734, #735 (audit-runner support work). Tier 3 demote to Low: #650 (/tools/ operator controls + log viewer), #195, #196 (stats dashboards), #229, #230, #231 (Tuning Loop C.0/C.1/C.2 chain — the longest open dependency chain on the board), #219 (Write tuning.md). Tier 3 rationale: launch-focus reprioritization; the post-launch reshape after Fly Public Launch + Release Validation ship should reconsider whether tuning + stats arc work returns to Medium. #377 (Open-source launch readiness epic) kept at Medium per Decision 15's epic-priority convention — its anchor role is already signaled by being IN the active milestone. Net effect: open priority distribution shifts from 1H / 19M / 16L / 5 NoPri to ~8H / ~12M / ~21L / 0 NoPri. (e) **New strategic issue #747** filed under Release Validation: "tracking: Docker + Fly parity validation matrix" (High). Captures the Docker leg of the operator's "both Docker and Fly.io as flawlessly as possible" mandate. Acceptance: a per-feature parity matrix document enumerates every user-visible feature surface (onboarding, board tabs, materials, settings, config, tools, docs, stats, prep-flow, every state-transition affordance, ntfy, IMAP rejection detection), with explicit "verified on Docker (populated soak instance / fresh-install test instance)" + "verified on Fly (operator's deploy or any user Fly stack post-#672)" cells carrying date + commit SHA. Referenced from `release-process.md` as a pre-tag gate for major-point releases. (f) **Up Next reset to top-3 launch-critical:** #746 (jobs-api14-indeed allowlist — sub-issue of #672), #673 (README Fly-first pivot — sub-issue of #672), #738 (continue-prep UX — most visible UI issue a stranger will hit on their first prep run). Five prior Up Next items (#737, #736, #735, #734, #729) demoted to Backlog within their respective milestones — they're priority-correct now but not the next-three-pulled. #377 Epic promoted from Backlog to In Progress to make the active arc visible alongside #672 (its acceptance test); priority kept at Medium per Decision 15. (g) **Decision 19 override deliberation captured for cold-review traceability:** the v0.30-named-milestone option was considered (operator's initial preference), advisor flagged the Decision 19 conflict, operator confirmed conformance with Decision 19 mid-session. The descriptive-naming convention is reaffirmed; future milestone names should remain scope-descriptive (`X + Y`, `X Hardening`, `X Maturity`, `X Validation`) rather than version-anchored (`v0.X.Y …`). Dependency graph re-checked: 4 issues, 5 edges, 0 cycles, 0 priority inversions, 0 orphans; critical path #231 → #230 → #229 unchanged (Tuning Loop chain, now all three at Low). Shape stays one coherent board (58 open issues after #747; no orthogonal streams; no split signal). Active milestones for the close-out arc: Fly.io Public Launch (m11) + Release Validation (m25) — both due in the same week — plus six deferred (Funnel + Triage UX, Ops Hardening, Tools Page Maturity, Stats Dashboards, Audit & Cleanup, Tuning Loop).

29. **Structural review (2026-05-23): Fly.io Public Launch closed; Release Validation closed; Post-Launch User Sunset promoted to active anchor; 9 orphans absorbed; jared CLI enforcement filed.** Seven changes. (a) **Fly.io Public Launch (m11) closed** — every sub-issue under epic #377 had shipped (the six Decision-25 sub-issues #669–#674, #648, #810 walk #2 follow-up, plus #746 jobs-api14-indeed allowlist). Epic #377 closed with handoff comment naming Post-Launch User Sunset as next anchor. (b) **Release Validation (m25) closed** — 11/12 items shipped in 5 days; the remaining #777 (Low, persistent waitlisted row fixture) was reassigned to Audit & Cleanup since it's a small ride-along rather than a release gate. Decision 28's "anchored by #747 in In Progress" framing was already drifted within hours of being written — #747 closed 2026-05-22T21:56Z, same day. RV description also scrubbed of a literal deployment-host reference (operator-topology leak per CLAUDE.md PII rules) replaced with the canonical `deployment host` placeholder. (c) **Post-Launch User Sunset (m26) promoted to active anchor.** Its blocker #816 (user stack export/import tool, High) closed 2026-05-23, making the milestone pullable; three sub-issues (#817 dual-track runbook, #818 user comms template, #819 stack archival) plus tracker #749 form the close-out work, plus newly-assigned #841 (in-app backup/restore retiring host-side tooling — natural PLUS fit). (d) **9-orphan absorption — eighth consecutive sweep with the same `gh issue create` / `jared file` without-milestone pattern (Decisions 17, 19, 22, 23, 24, 27, 28, 29).** Assignments: #835 (High, Node 24 CI deadline 2026-06-02) → Ops Hardening; #843 (Low, Node 20 deprecation June 2026) → Ops Hardening; #837 / #838 / #839 / #840 (#641 notification follow-ups) → Audit & Cleanup; #830 (Medium, confirmation toast) → Funnel + Triage UX; #833 (Medium, onboarding Phase 5 auto-emit) → Audit & Cleanup; #841 (Medium, in-app backup/restore) → Post-Launch User Sunset; #777 → Audit & Cleanup (moved out of Release Validation per (b)). The four `big-idea` orphans (#94, #342, #378, #815) stay correctly orphaned. (e) **#835 deadline-vs-milestone caveat:** Node 24 CI upgrade has a hard 2026-06-02 upstream deadline, before Ops Hardening's deferred 2026-06-28 due date. Per Decision 25 the deferred milestones don't pull new work, but #835's High priority and named deadline communicate the override; the item should be picked up in flight as a one-off exception. (f) **Tooling response to recurring orphan pattern:** filed [brockamer/jared#238](https://github.com/brockamer/jared/issues/238) "jared file: enforce --milestone (or explicit --no-milestone) to stop the orphan stream" — Up Next, Medium. Proposes adding `--milestone NAME` and `--no-milestone` flags to `jared file` and refusing to file without one. Fixes the root cause across all jared-using projects, not just findajob. (g) **Dependency hygiene:** removed stale edge #819 → #816 (blocker closed 2026-05-23). Dependency graph remains clean (3 issues, 4 edges, 0 cycles, 0 priority inversions, 0 orphans; critical path #231 → #230 → #229 unchanged — Tuning Loop, deferred). Shape stays one coherent board (33 open issues after the close + reassignments + 9 orphan adoptions; no orthogonal streams; no split signal). Active milestones for the close-out arc: Post-Launch User Sunset (m26, active anchor) + six deferred (Funnel + Triage UX, Ops Hardening, Tools Page Maturity, Stats Dashboards, Audit & Cleanup, Tuning Loop). Funnel + Triage UX (2026-06-24) named as the natural successor active anchor when the unfreeze window opens ~2026-06-15.

28. **Structural review (2026-05-22): Fly.io Public Launch closeable; Release Validation promoted to active anchor; Post-Launch User Sunset milestone filed.** Seven changes. (a) **#672 (FPL milestone-acceptance test) closed earlier today** alongside 13 other issues in the closure wave. Fly.io Public Launch (m11) is closeable — 26/28 closed; only #377 (Epic) and #810 (walk #2 sub-findings, 5 doc-only items filed today) remain. The milestone closes the moment #810 ships. (b) **Release Validation (m25) promoted to active anchor** per the operator's Q1 approval — active phase framing transitions from "Fly.io Public Launch anchor" (Decision 25) to Release Validation, with FPL in active-but-closing-out state. (c) **Post-Launch User Sunset (m26) filed** per the Decision 26 trigger firing today ("surface when #672 is ~1 week from acceptance"). Due 2026-06-15. Deliverable: "The self-onboarded user docker stacks are migrated to per-user Fly accounts via a verified export/import tool, the release process runs dual-track docker + Fly until cutover completes, and the docker stacks on the deployment host are stopped and archived." Four sub-issues filed: #816 (migration tool, High, blocks #819), #817 (dual-track release runbook, Medium), #818 (user comms template, Medium), #819 (stack archival, Medium). (d) **Loose-ends harness cluster absorbed into Release Validation** (operator delegated Q2 to Claude). The cluster (#756, #777, #778, #779 plus the already-milestoned #734) did NOT get its own focused milestone because (i) Release Validation's deliverable explicitly names the loose-ends audit, and (ii) adding a 9th milestone with 6 already deferred would add noise without signal. (e) **Orphan absorption (8 items)** — same recurring `gh issue create` without `--milestone` drift as Decisions 17/19/22/23/24/27: #748 → Fly.io Public Launch (bug in launch path itself); #749 → Post-Launch User Sunset; #756 / #777 / #778 / #779 → Release Validation; #765 → Funnel + Triage UX (adapter fix from #601 validation); #798 → Audit & Cleanup (#737 follow-up observability). (f) **Metadata drift fixes:** #815 received `big-idea` label (title said "Big idea:" but label was missing) and Priority Medium → Low per project-board.md big-idea-always-Low convention; #229 title edited to drop vestigial `(cost-tracking moved to #48)` parenthetical (cost-tracking arc shipped in CCH per Decision 22). (g) **Pre-existing roadmap drift from Decision 27 fixed:** Reduce per-prep cost (m21) and Board Transition Coherence (m24) — both closed in Decision 27 — were still listed as open milestones in `## Post-GA horizon`; moved to `## Shipped and retired milestones`. Notable absence of priority drift this sweep — first review in many sweeps where every open item has Priority set; Decision 27's priority overhaul plus today's 14-issue closure wave appears to have cleared the no-Priority bucket entirely. Dependency graph adds 1 new edge (#819 → #816 migration gate); remains clean (5 issues, 6 edges, 0 cycles, 0 priority inversions, 0 orphans; critical path #231 → #230 → #229 unchanged). Shape stays one coherent board (35 open issues after the 4 new file-ups; no orthogonal streams; no split signal). Active milestones for the close-out arc: Release Validation (m25, active anchor) + Fly.io Public Launch (m11, closing out) + Post-Launch User Sunset (m26, named but not yet active) + six deferred (Funnel + Triage UX, Ops Hardening, Tools Page Maturity, Stats Dashboards, Audit & Cleanup, Tuning Loop). The six-deferred unfreeze window (~2026-06-15 per Decision 25) lines up with m26's due date.

### Scope out (explicit)

- Per-user identity / RBAC inside findajob — Decision 16 added shared-secret auth, not identity.
- Separate rclone-replacement project.
- Manual RAG source document editing.
- Alternative LLM provider exploration.

---

## Shipped and retired milestones

- **Reliable Materials** (m1) — shipped; all acceptance criteria closed.
- **Expanded Coverage** (m2) — retired 2026-04-18; scope pivoted into the generalization work (first external user) now tracked under General Availability.
- **Feedback Loop v2** (m3) — closed 2026-04-18; initial scope shipped.
- **Multi-Tenancy Foundations** (m10) — shipped 2026-05-06, ahead of due 2026-05-18 (Decision 21). Anchored by epic #338. Per-user key isolation (#339), in-app onboarding chat (#336), multi-tenant operator dashboard (#333), Gmail IMAP per-stack credentials (#330), cron stagger (#344), onboarding paste-back polish (#283/#287/#372), discoverer schema fix (#345). All external users onboarded with isolated credential paths.
- **Cost + Credentials Hardening** (m6) — shipped between 2026-05-06 and 2026-05-09, ahead of due 2026-05-22 (Decision 22). 10 issues closed. Per-LLM-call cost tracking, dashboard burn-rate widget, credential isolation hardening, prefilter rule cleanup.
- **OpenRouter Native Migration** (m12) — shipped 2026-05-09 on schedule (Decision 22). 4 issues closed; anchored by epic #469 with Phase 1/2/3 children #470/#471/#472. aichat-ng fully replaced by `findajob.llm.openrouter`; prompt caching active on Opus-4.7; `cost_log.cost_usd` from `response.usage.cost`; calibration multiplier stack retired.
- **Reduce per-prep cost** (m21) — shipped during the v0.27.x arc, closed in Decision 27 (2026-05-19). 4 issues closed. Per-prep cost investigated and bounded back toward historical baseline after the 16384-max_tokens spike (#622 / #640); `prep_application.py` parallel-LLM-calls work (#496) shipped.
- **Board Transition Coherence** (m24) — shipped during the v0.27.x arc, closed in Decision 27 (2026-05-19). 9 issues closed. State-transition affordances across the board reconciled — every transition has a single canonical POST handler in `findajob.web.routes.board_actions` and the dashboard table matches the documented stage lifecycle.
- **Fly.io Public Launch** (m11) — closed 2026-05-23 in Decision 29, on schedule (due 2026-05-29). 29 issues closed. Average-person-on-Fly path validated end-to-end: install-fly.md, cost transparency doc, cost-cap onboarding, README Fly-first pivot, API-key acquisition audit, unaffiliated-user walkthrough (#672) acceptance test plus walk #2 sub-findings (#810), jobs-api14-indeed allowlist generalization (#746), and epic #377 closeout. Self-host via docker-compose remains documented as a power-user surface but is no longer the strategic anchor.
- **Release Validation** (m25) — closed 2026-05-23 in Decision 29, ahead of due 2026-06-04. 11 issues closed. Loose-ends audit harness shipped (#572 plus #756/#777/#778/#779/#734/#735/#736 phase-2 cluster); continue-prep UX (#738), excluded_employers UI (#729), and the Docker + Fly parity validation matrix (#747) all shipped. Decision 28's "anchored by #747 in In Progress" framing turned out to be drifted within hours of writing (closed same day). #777 (loose-ends fixture) reassigned to Audit & Cleanup as a ride-along rather than a release gate.
- **Code-health track (M-series)** — six small refactor/quality milestones shipped in parallel to the feature arc between approximately 2026-04-30 and 2026-05-09 (Decision 22 surfaces them retroactively): Test Baseline (m15, 8 closed), DB Helper + utils.py Split (m17, 4 closed), Versioned Migration Runner (m18, 1 closed), Background-Task Observability (m19, 1 closed), Type-Gate Tightening (m20, 1 closed), Pipeline-Script Extraction (m16, 5 closed). Convention going forward: code-health milestones get one-line acknowledgement here per ship rather than a full milestone section, since their scope is mechanical and their narrative weight is low.

## Post-GA horizon

Post-GA Hardening was a single undated grab-bag milestone through 2026-04-24; a structural review that day split it into four dated release milestones so the Roadmap view renders past GA. The 2026-04-29 reviews added Multi-Tenancy Foundations between GA and Cost + Credentials Hardening, and twice compressed dates — see Decisions 17 and 18 — landing at the current ~30-day window. The 2026-05-01 review (Decision 19) renamed milestones to drop the v0.9/v1.1/etc. prefixes, eliminating the codename-vs-semver collision that had emerged once v0.9.x release tags started shipping while the v0.9 milestone was still mid-flight. Deliverable sentences are authoritative — if a proposed issue doesn't fit exactly one, it belongs in NO_MILESTONE (big-idea) or in a new milestone, not wedged into an existing one.

**Milestone names are now deliverable-anchored, not version-numbered.** Chronological release order after Decision 29's reshape: GA → Multi-Tenancy Foundations → OpenRouter Native Migration → Cost + Credentials Hardening → Reduce per-prep cost → Board Transition Coherence → Fly.io Public Launch → Release Validation → **Post-Launch User Sunset** (active anchor) → Funnel + Triage UX → Ops Hardening → Tools Page Maturity → Stats Dashboards → Audit & Cleanup → Tuning Loop. Post-Launch User Sunset took over the active anchor slot from Release Validation on 2026-05-23 when both Fly.io Public Launch and Release Validation closed (Decision 29); the six concurrent milestones deferred in Decision 25 remain deferred until the close-out arc concludes (~2026-06-15, lining up with Post-Launch User Sunset's due date). Funnel + Triage UX (2026-06-24) is the chronologically-next milestone and the natural successor active anchor at unfreeze. Semver release tags (v0.9.x, v0.10.x, v1.0.0, …) float on the release-process schedule independently of milestone names.

- **[Funnel + Triage UX](https://github.com/brockamer/findajob/milestone/9)** (due 2026-05-25) — "More job sources flow into the funnel, and the operator's daily triage loop makes every candidate row actionable in one click with prior-application context inline." Scheduled ahead of Stats Dashboards / Tuning Loop / Ops Hardening because funnel/UX friction is the active-operator pain.
- **[Ops Hardening](https://github.com/brockamer/findajob/milestone/8)** (due 2026-05-29) — "Fresh-install smoke is CI-gated, pipeline.jsonl rotates, DB schema migrates cleanly, folder/DB drift is detectable on demand, and write paths have e2e coverage." Can ship in parallel with Stats Dashboards / Tuning Loop; date is outside-in.
- **[Tools Page Maturity](https://github.com/brockamer/findajob/milestone/22)** (due 2026-06-02) — "The /tools/ page evolves from a prompt-tile launcher into a working operator surface: an interactive exclusion rule editor (#653), manual operator controls + pipeline.jsonl log viewer (#650), and verified Open-in-Claude prefill behavior (#654)." Filed in Decision 24 to gather the /tools/-surface work that emerged after #150's prompt-tile MVP shipped; follows the M-series small-focused-milestone convention from Decision 22.
- **[Stats Dashboards](https://github.com/brockamer/findajob/milestone/13)** (due 2026-06-10, slipped from 2026-05-27 in Decision 24) — "The /stats/* pages render score distribution, rejection breakdown, application throughput, and feedback-effectiveness charts from live data." Visualization sibling of Tuning Loop. Date slipped from 2026-05-27 to accept calendar reality (0/5 closed with 13 days remaining at sweep time); #606 (cost burn-rate widget bug) moved out into Funnel + Triage UX as part of the same decision.
- **[Audit & Cleanup](https://github.com/brockamer/findajob/milestone/23)** (due 2026-06-12) — "Half-working notification surfaces, dead-code drift, release-process cruft, board state-transition incoherence, and accumulated capability-without-UI gaps are surfaced and either fixed in-place or filed as concrete follow-ups." Filed in Decision 24 to gather audit-class issues (#641 ntfy, #649 release-process, #651 poll_flags dead-code, #662 board state-transition coherence, #572 periodic UX audit) that surfaced during the post-GA close-out arc; follows the M-series convention from Decision 22.
- **[Tuning Loop](https://github.com/brockamer/findajob/milestone/14)** (due 2026-06-15) — "The pipeline recommends scorer tunes from accumulated user-behavior metrics, surfaced as a one-click approval flow that the operator never has to invent manually." Anchored by epic #228 with C.0/C.1/C.2 children.
- **[Post-Launch User Sunset](https://github.com/brockamer/findajob/milestone/26)** (due 2026-06-15, **active anchor** per Decision 29) — "Self-onboarded docker stacks are migrated to per-user Fly accounts via a verified export/import tool, the release process runs dual-track docker + Fly until cutover completes, and the docker stacks on the deployment host are stopped and archived." Promoted to active anchor 2026-05-23 in Decision 29 once #816 (migration tool, High) shipped — the blocker on the rest of the cutover work. Open sub-issues: #817 (dual-track release runbook), #818 (migration comms), #819 (stack archival), plus #749 (cold-cutover migration playbook tracker) and #841 (in-app backup/restore, retires host-side tooling — assigned 2026-05-23). Due date intentionally aligned with the six-deferred-milestone unfreeze window so the close-out arc concludes coherently.

When a single milestone is the strategic anchor (e.g., Multi-Tenancy Foundations after GA closes), add a `## Active milestone: <name>` section with the same shape (goal, acceptance, remaining scope, decisions, scope-out). Move the outgoing milestone's section up to `## Shipped and retired milestones`. When multiple milestones run concurrently with no single anchor (the post-MTF phase, per Decision 21), use the `## Active phase — parallel close-out` framing instead and list the active milestones in this Post-GA horizon section.

---

## Far-future wishlist

Filed as `big-idea` issues on the board so they're trackable without cluttering the active roadmap. Each becomes worth promoting only after the prerequisite milestone arc has demonstrated real-world signal.

**Multi-user scaling** — *promoted to active roadmap 2026-04-29 as Multi-Tenancy Foundations milestone (Decision 17).* No longer wishlist.

**Open-source launch readiness** — [#377](https://github.com/brockamer/findajob/issues/377), shipped 2026-05-23 (Decision 29). *Promoted to active milestone 2026-05-06 (Decision 22); renamed and refocused as Fly.io Public Launch on 2026-05-15 (Decision 25); closed alongside the FPL milestone on 2026-05-23.* The average-person-on-Fly path is the validated outcome. Self-host docker-compose remains documented as a power-user surface but is no longer the headline.

**Outcome-driven self-tuning** — [#378](https://github.com/brockamer/findajob/issues/378). Once Tuning Loop + Stats has accumulated 3–6 months of behavioral signal, evaluate whether the system can propose its own scorer tunes (operator approves with one click, never auto-applied). Substrate is the C.0/C.1/C.2 work in epic #228.

**Cross-user signal (opt-in)** — [#379](https://github.com/brockamer/findajob/issues/379). Once 3+ external users have multi-month data, evaluate value of opt-in telemetry surfacing career-cluster funnel benchmarks. Privacy model dominates implementation; do not start before users say "yes please" enthusiastically.

**Hosted variant** — Only if demand materializes from the open-source launch. Not in scope until then. No issue filed.

_(Original wishlist migrated from issue #88, closed 2026-04-21; refreshed 2026-05-01 — concrete items now have issue numbers.)_

---

## Conventions

- **One active milestone at a time** gets a full `## Active milestone` section. When in parallel close-out (multiple milestones shipping concurrently), use `## Active phase — parallel close-out` instead and list the milestones in `## Post-GA horizon` (Decision 21).
- **Phases are a narrative layer on top of issues.** An issue can belong to exactly one phase; a phase is a group of issues that ship together or in close sequence.
- **Issue numbers are not embedded in the phase arc** — they drift. Look up execution units via the milestone page.
- **Decision log is append-only.** Numbers are stable references.
- **Close the meta-issue** that originally held this narrative with a pointer here. The meta-issue was scaffolding for the doc, not a work item.
